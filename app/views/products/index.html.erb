<div class = "row product-list">
  <div class="col-md-10">
    <% if @products.any? %>
      <%= render @products %>
      <%= will_paginate %>
    <% else %>
      <p class="text-info"><%= t "product.no_available" %></p>
    <% end %>
  </div>
</div>
